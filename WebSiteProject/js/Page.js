const allText = document.querySelector(".h1");

function reText() {
   for (let i = 0; i < allText.length; i++) {
       if (allText.textContent == 10){
           console.log(dolu);
       }
        
   }
}

reTex